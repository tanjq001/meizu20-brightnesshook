# 亮度曲线调节（Brightness Curve Hook）

**Xposed / LSPosed 模块 · 版本 1.0 · 免费 · 作者：酷安 折翼之舞007**

---

## 一句话简介

在系统框架层精细调节自动亮度曲线，解决自动亮度「过暗、不灵敏、忽明忽暗」的痛点。

## 功能特性

- 🎯 **光感输入拦截**：直接修改光线传感器读数，让系统按你的意愿调节亮度
- 🌊 **中值滤波 + 延迟输出**：对光感信号做中值滤波并延迟输出，抑制脉冲噪声，告别明暗跳跃
- 📊 **三段亮度倍率**：低/中/高亮度分别独立调节，暗处增亮、亮处压暗随心所欲
- ⚡ **防抖时间调节**：自定义变亮/变暗的响应速度，灵敏还是沉稳由你决定
- 🔧 **实时生效**：所有参数通过 `setprop` 修改，**无需重启**立即生效

## 使用条件

- ✅ 已 root（KernelSU / Magisk）
- ✅ 已安装 LSPosed（Zygisk 版）
- ✅ Android 14（在 MEIZU 20 / Flyme 10.5 上开发测试）

## 快速上手

1. 安装本模块 APK
2. LSPosed → 模块 → 启用「亮度曲线调节」，作用域勾选**系统框架**
3. 重启手机
4. 用 `setprop` 调参（实时生效，重启保留）

```bash
# 整体调亮（系统以为环境更亮 50%）
su -c 'setprop persist.brctl.lux_inc 1.5'

# 中值滤波 + 延迟 2 秒输出（抑制明暗跳跃）
su -c 'setprop persist.brctl.lux_min 10'

# 加快响应（缩短防抖）
su -c 'setprop persist.brctl.darken_debounce 1500'
```

## 全部参数

| 参数 | 默认 | 说明 |
|---|---|---|
| `lux_inc` | 1.3 | 光感倍率，>1 更亮 |
| `lux_min` | 0 | 光感最小值（lux） |
| `smooth_window` | 4000 | 滤波时间窗（毫秒） |
| `filter_mode` | median | 滤波方式：median 中值 / mean 均值 |
| `low_inc` / `mid_inc` / `hig_inc` | 1.0 | 低/中/高亮度倍率 |
| `low_max` / `mid_max` | 30 / 60 | 亮度档位边界（%） |
| `brighten_debounce` | 1000 | 变亮防抖（毫秒） |
| `darken_debounce` | 2000 | 变暗防抖（毫秒） |

完整说明见模块仓库 README。

## 更新日志

**v1.0**
- 光感输入拦截（倍率 + 中值滤波 + 延迟）
- 亮度输出三段倍率
- 防抖时间反射修改
- 全部参数系统属性化，实时生效

## 注意事项

- 本模块 hook 系统框架，作用域务必只勾「系统框架」
- 卸载/停用前建议先重置参数：`resetprop --delete persist.brctl.*`
- 不同机型/系统的 hook 点可能有差异，仅保证在开发机型上可用

## 免责声明

本模块免费开源，仅供学习研究。因使用本模块导致的任何设备问题，作者不承担责任。
